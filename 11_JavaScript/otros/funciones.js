
// Crear una función para sumar (2 números)
// function sumar () {}
const sumar = (num1, num2) => {
    return num1 + num2;
}

// Crear una función para restar (2 números)
const restar = (num1, num2) => {
    return num1 - num2;
}

// Módulos / Imports
module.exports = {
    sumar,
    restar
}
